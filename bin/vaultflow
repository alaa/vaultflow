#!/bin/bash
set -e

docker run -e CONSUL_ADDR=$CONSUL_ADDR \
           -e VAULT_TOKEN=$VAULT_TOKEN \
           -e VAULT_ADDR=$VAULT_ADDR   \
           -u $(id -u $USER)           \
           -v $(pwd):/root             \
           alaa/vaultflow "$@"
